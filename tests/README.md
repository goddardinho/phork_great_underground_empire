# Tests Directory

This directory contains all unit tests for the project. To run all tests:

```
python3 -m unittest discover tests
```

Or run individual test files:

```
python3 tests/test_room_flags.py
```
